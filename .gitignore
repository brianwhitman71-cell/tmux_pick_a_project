# Per-host config — each machine keeps its own untracked copy (install.sh seeds
# these from the .example files). Keeps real project names/paths/fleet out of git.
projects.tsv
peers

# editor / OS cruft
*.swp
*~
.DS_Store
